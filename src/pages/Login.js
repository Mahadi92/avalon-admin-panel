import React from 'react';
import Form from '../components/Form';

const Login = () => {

    return (
        <section id="loginPage">
            <Form type="login" />
        </section>
    );
};

export default Login;